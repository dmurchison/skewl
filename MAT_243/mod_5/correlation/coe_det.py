from scipy import linregress
from scipy import simplelinearregression

# The difference between the two is that linregress returns the slope, intercept, r-value, p-value, and standard error of the estimate,
# while simplelinearregression returns the slope, intercept, and r-value.

# The DataFrame.corr function 
